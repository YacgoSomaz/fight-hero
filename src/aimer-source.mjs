// Direct crop of Aimer (symbol 1431), frame 1, exported from the original
// SWF with FFDec.  The source MovieClip is centred on its 13×13 registration
// point, so callers must place this image around the actual mouse position.
export const ORIGINAL_AIMER = Object.freeze({
  source: './public/assets/original-swf/aimer-1431-frame1.png',
  sourceSymbol: 1431,
  sourceFrame: 1,
  width: 26,
  height: 26,
  origin: { x: 13, y: 13 },
});
