# 《战火英雄》网页 1:1 复刻总目标与实施计划

更新时间：2026-07-22。本文定义最终目标，不以“能打开”“能走路”或“画面相似”替代完成。

## 1. 最终目标

在本地网页运行环境中，基于已解包原 SWF 的素材、Display List、时间轴、AS3/P-code、地图节点和规则数据，复刻用户能实际玩到的原版体验。完成时应满足：

1. 任何已显示的图像、音频、动画和 UI 均可追溯到原 SWF 导出或其原始运行时关系；不以手绘、临时 CSS 图形或“看起来差不多”的替代物填补原始内容。
2. 菜单、关卡、模式、角色、武器、AI、物理、HUD、任务推进和存档的行为有原始 AS3/时间轴/节点证据，且网页行为与之对照验收。
3. 每个完成项都同时具备：原始证据位置、网页实现位置、自动测试、人工对照截图或输入回放结果。
4. 未迁移、证据不足或未对照的部分必须明确标注为未完成；绝不能用“可运行”或“私用”将其称作 1:1。

## 2. 证据优先级

发生冲突时按下列顺序取证：

1. 原 SWF 运行结果和原版可录制画面；
2. 原 SWF 的 AS3、P-code、时间轴标签、Place/Remove Display List；
3. FFDec 直接导出的 symbol、位图、声音、节点和矩阵；
4. `assets/reverse/` 与 `private-assets/` 中保留的解析报告；
5. 现有网页代码和测试。

网页代码不是原始行为的证据。若原件尚未找到，工作项应留在“待解包/待验证”，而不是自行补写一个相似机制。

## 3. 子系统实施清单

### A. 证据台账（第一优先级）

为下列每个实体建立一行台账：`原始 symbol/AS3 入口 → 导出资源/数据 → 网页模块 → 自动测试 → 原版对照截图 → 状态`。

- 菜单帧、按钮命中区、标签页、锁定/解锁状态；
- 14 个地图视觉 ID、地图物理 ID、昼夜变体、三层图像和 wallMC；
- UnitMC、各兵种皮肤、HUD、准心、血条、弹药、经验条；
- `Stats_Guns` 的每一把枪、枪械子时间轴和声音；
- `Stats_Campaign`、挑战目录、任务和胜利/失败条件；
- `Movement`、`AI`、`Arena`、`Unit`、`MatchSettings` 的关键函数与数据关系。

完成标准：每个已经向用户开放的功能都有完整台账；台账缺口会阻止其被标成完成。

### B. 原始菜单与全部入口

范围：Home、Play、Soldiers、Options、Medals、Tips、Version、Campaign、Challenges、Quick Match。

- 必须复用原始菜单帧，而非重新设计一个前端菜单；
- 每个可见按钮必须对应 Menu.as 的真实 action、坐标和状态变化；
- Soldiers/Options/Medals/Tips/Version 不能仅是无效按钮；若未解出行为则必须继续解包；
- 战役 15 项、挑战 15 项、快速对战的地图/模式/分数/士兵限制/技能/连杀/难度循环必须一致；
- 菜单音乐、点击音、存档读取、解锁状态和返回路径必须可验证。

验收：逐页原版与网页并排截图；对每一个可见按钮进行一次点击回放并记录目的状态。

### C. 全部地图、镜头和物理数据

地图范围：`tut`、`foundry`、`foundry2`、`train`、`train2`、`plane`、`plane2`、`swamp`、`swamp2`、`cave`、`cave2`、`dropship`、`missile`、`missile2`。

每张地图必须完成：

- `BgSky`、`Bg`、`Arena` 图层的原始裁切、注册点、缩放、遮挡顺序和镜头滚动；
- Arena 的 `wallMC` 或 `NodePhysBox` 碰撞，禁止用通用地板取代；
- 出生点、waypoint、action（跳跃/攀爬等）、pickup、CTF 旗帜、DOM 占领点；
- 昼夜变体只改原版实际改变的层，不复制出一套脱节物理；
- 从边缘、坑、上坡、下坡、平台底部、攀爬边沿逐项验证玩家和 AI。

验收：每图至少覆盖出生、连续移动、跳跃、攀爬、坠坑、敌人追击、镜头边界和对应模式目标的输入回放。

### D. 角色、动画、瞄准和 HUD

- UnitMC 必须按原 Display List 和帧标签播放：待机、跑、倒跑、跳、坠落、蹲、起身、蹲走、攀爬、重落地、受击、死亡、复活；
- 头、躯干、双臂、双腿和枪械的 holder/matrix 必须保留，鼠标只驱动原版会驱动的部件；
- 所有兵种皮肤、随机角色逻辑和可选兵种须来自原资料；
- 头顶血条、底部职业/生命/等级/经验、弹匣、备用弹药、准心、命中反馈必须复用原 HUD symbol；
- Canvas 合成不可靠时，可用 DOM 承载原始图像，但其坐标、遮挡、翻转和状态必须与单位/镜头同步。

验收：逐动作帧与原版截图对照；移动、反向瞄准、左键开火、换弹、贴墙蹲起、自动攀爬和复活均须人工回放。

### E. 武器、战斗、音效和特效

- 将 `Stats_Guns.addGun` 的完整武器定义导入可查询数据表；
- 每枪实现原始弹匣、备用弹药、射速、后坐、散布、射程、伤害、换弹和子时间轴；
- 枪口、弹道、命中、爆炸、掉落、拾取、音效和粒子从原素材/代码取值；
- 不得只用 M4 的固定演示替代完整枪表。

验收：按武器表生成回归用例；关键枪械逐帧对照开火与换弹，全部枪械至少验证可装备、可开火、可换弹和伤害规则。

### F. AI、运动和五种对战规则

- Movement：脚底/头顶/侧向探针、坡面、小/大攀爬、贴墙蹲起、重力、平台和子弹墙体碰撞；
- AI：索敌、视线、瞄准平滑、开火概率、waypoint 图搜索、action 框、跳跃、脱困、随机配置；
- 规则：Deathmatch、Team Deathmatch、Capture the Flag、Domination、Juggernaut 的出生、计分、旗帜、占领、角色转换、胜利和结束；
- 战役/挑战：每关的敌人/任务/解锁/失败/结算，而不是将目录项全部折叠成同一个死亡竞赛。

验收：每条 AS3 分支至少映射一个单元/集成测试；每个模式由真实输入回放到得分或结束状态。

## 4. 质量门槛

每次提交涉及上述系统时：

1. 先新增针对原版行为的 RED 测试；
2. 采用原始资源或证据实现最小修复；
3. 跑 `npm test` 和 `npm run test:coverage`；
4. 对可视变更做浏览器截图/输入回放；
5. 更新台账、解包报告和交接文档；
6. 只有全部证据齐全的项目才可标为完成。

## 5. 分阶段交付物、完成定义与验收记录

下面的阶段不是“建议做做看”，而是后续每一次实现、测试、提交和交接必须对应的工作分解。某阶段没有达到该阶段的验收条件，只能写“进行中/阻断”，不能因为画面能显示就写“完成”。

### 阶段 0：原版基线与可重复对照

**目的**：让后续所有判断都有同一份原始依据，避免拿网页自身的效果当成正确答案。

| 项目 | 必须产物 | 验收方法 | 当前状态 |
| --- | --- | --- | --- |
| 原件身份 | SWF 文件路径、SHA-256、文件大小、FFDec 版本/JRE 版本 | 一条脚本可重新打印哈希与工具版本 | 部分已有，需集中登记 |
| 舞台基线 | 原 SWF `800×600`、30fps、注册点和舞台缩放规则 | 浏览器画布与原版基准图按同一坐标标尺叠图 | 已确认源数据；网页尚未改为最终 4:3 舞台 |
| 原版对照集 | 首页、菜单、每张地图出生、各角色动作、HUD、每种模式的原版截图/短录屏 | 每张图都附采集时间、输入步骤、原版关卡/模式 | 不完整 |
| 解包可重现 | symbol、timeline、AS3/P-code、shape/sprite/sound 导出命令与输出目录 | 清空临时导出目录后可重新得到相同核心结果 | 已有 FFDec 深解包，需补完整命令索引 |
| 证据台账 | 每个网页功能能回指 symbol/class/frame/资源文件 | 随机抽查 10 项均可从网页模块找到原始来源 | 已建立，持续补全 |

**阶段 0 的禁止项**：不能把用户截图裁一块贴在页面上冒充可交互 UI；不能将“当前网页截图”作为原版参考；不能因为资源已经导出就推断行为已经解明。

### 阶段 1：原始首页、主菜单与完整可达流程

**目标流程**：启动 → 原标题/新闻/主菜单 → Play → Campaign / Challenges / Quick Match → 设置并开始 → 结算 → 返回上级；Soldiers、Options、Medals、Tips、版本信息也必须有真实页面或原版明确的不可用状态。

| 子项 | 原版证据要找什么 | 网页要实现什么 | 必须测试 |
| --- | --- | --- | --- |
| 首页帧 | 主时间轴帧标签、原背景/Logo/新闻框、按钮 hit area | 复用原画面元素、原排列/坐标/层级、焦点与音效 | 所有可见按钮有唯一可点命中区；截图对比 |
| Play 分支 | `Menu.as` action、按钮可见条件 | 进入模式页，不能靠隐藏热区跳到某一关 | 连续点击回放，路径与预期路由一致 |
| Campaign | 15 个目录项、锁定/解锁/任务文本/图标 | 每项展示原序号、名称、状态，点击进入对应关卡规则 | 15 项逐项可打开；锁定项不可误进 |
| Challenges | 15 个目录项、限制条件/难度/奖励 | 每项独立绑定，而非统一跳 Foundry | 15 项逐项可打开并显示不同配置 |
| Quick Match | 地图、模式、分数、士兵、技能、连杀、难度循环 | 可切换并生成真正影响比赛的 `MatchSettings` | 每个控件改变状态；开始后参数传入战斗 |
| 其他页 | Soldiers/Options/Medals/Tips/Version 时间轴或 AS3 | 原功能、原锁定提示，或有明确“尚未迁移”的禁用状态 | 不能存在看得到却无反馈的按钮 |
| 返回/存档 | 本地保存和解锁字段的原类/变量 | 返回后保持选择；重载后按原规则保留进度 | 两轮进入/返回/刷新回归 |

**完成定义**：菜单截图像原版只是最低条件；真正完成是每一个可见按钮都有可测试的目标状态，并且菜单选项会传递到实际战斗配置。

### 阶段 2：14 张地图的画面、镜头、碰撞与节点

每张图必须独立完成，不允许“同一张 Foundry 能玩”就把其他 13 张算进完成范围。

| 图号 | 地图 ID | 视觉层检查 | 物理/节点检查 | 状态 |
| --- | --- | --- | --- | --- |
| 01 | `tut` | 资源、裁切、前后景、原坐标 | 出生、平台、墙体、教程节点 | 待逐图验收 |
| 02 | `foundry` | 资源、裁切、前后景、原坐标 | `wallMC`、坡、坑、攀爬、出生 | 有基础，未全验收 |
| 03 | `foundry2` | 资源、裁切、前后景、原坐标 | 对应 wall/node/变体规则 | 待逐图验收 |
| 04 | `train` | 资源、裁切、前后景、原坐标 | 车厢/平台/节点 | 待逐图验收 |
| 05 | `train2` | 资源、裁切、前后景、原坐标 | 对应 wall/node/变体规则 | 待逐图验收 |
| 06 | `plane` | 资源、裁切、前后景、原坐标 | 飞机平台、边界、出生 | 待逐图验收 |
| 07 | `plane2` | 资源、裁切、前后景、原坐标 | 对应 wall/node/变体规则 | 待逐图验收 |
| 08 | `swamp` | 资源、裁切、前后景、原坐标 | 地面、障碍、出生 | 待逐图验收 |
| 09 | `swamp2` | 资源、裁切、前后景、原坐标 | 对应 wall/node/变体规则 | 待逐图验收 |
| 10 | `cave` | 资源、裁切、前后景、原坐标 | 洞穴边界、平台、出生 | 待逐图验收 |
| 11 | `cave2` | 资源、裁切、前后景、原坐标 | 对应 wall/node/变体规则 | 待逐图验收 |
| 12 | `dropship` | 资源、裁切、前后景、原坐标 | 船体、落差、出生 | 待逐图验收 |
| 13 | `missile` | 资源、裁切、前后景、原坐标 | 导弹井、平台、出生 | 待逐图验收 |
| 14 | `missile2` | 资源、裁切、前后景、原坐标 | 对应 wall/node/变体规则 | 待逐图验收 |

每张图固定执行以下 **10 条地图回归**：

1. 从每个出生点开始，镜头不会突然跳切或把角色留在画面外；
2. 向左/右连续跑到边界，背景、前景和角色遮挡顺序正确；
3. 走上坡和下坡，双脚贴地，没有浮空、抖动或穿透；
4. 撞矮障碍，发生原版对应的跳跃/攀爬/阻挡，而不是卡进模型；
5. 贴墙蹲下再松开，能够站起并能跳跃；
6. 从坑中沿两个方向均可按原规则脱困，AI 也不会永久停在坑内；
7. 跳到每个平台、从平台底部经过、从边缘落下均无穿模；
8. 子弹对墙体、平台和前景遮挡遵循原物理层；
9. 在该图放入 AI 后，至少覆盖一段正常寻敌路线与一段 action/跳跃路线；
10. 以原版同坐标截图叠图检查图层裁切、相机范围和 HUD 安全区。

**完成定义**：14 张图各有资源加载测试、节点/墙体数据测试、浏览器输入回放和一份差异截图。只修“首关能加载”不算地图阶段完成。

### 阶段 3：UnitMC、角色状态机与武器持有

角色不能再以整张静态 PNG 平移替代。实现要保留原 `UnitMC` Display List 的部件关系，至少包括头、躯干、前/后臂、上/下腿、脚和枪械 holder。

| 状态组 | 必须复用/解析的证据 | 页面上的验收动作 |
| --- | --- | --- |
| 初始化/待机 | UnitMC 默认帧、皮肤、枪械挂点 | 原出生姿势、左右翻转、持枪位置一致 |
| 跑动/倒跑 | 原帧标签、脚/腿矩阵、帧速 | 连跑 3 秒时步态循环无滑步，反向时不是镜像平移 |
| 跳跃/坠落/落地 | 起跳、空中、重落地帧与速度规则 | 连续起跳、落平台、落地后过渡正确 |
| 蹲伏/蹲走/站起 | 短身形帧、头顶探针和站起判定 | 贴墙蹲下、松键、跳跃、无法站起的边界均符合原版 |
| 攀爬/越障 | 边缘判定、攀爬帧、holder 变化 | 撞到可攀边缘自动上爬，非可攀边缘不误触发 |
| 瞄准/开火/后坐 | Aimer、Unit、武器臂/枪的矩阵和 recoil | 鼠标 360° 指向，枪口朝向、手臂、准星、后坐同步 |
| 换弹/受击/死亡/复活 | 对应帧标签/状态转换/可见性 | 不能在死亡或换弹时移动/射击；复活恢复正确状态 |

现有进展只能记为：Medic 的原始部件已经可以通过 DOM 显示，且 10 个部件的初始层级与瞄准驱动已做浏览器确认；**它不代表所有上述状态已经还原**。

### 阶段 4：HUD、准星、头顶血条和原始舞台坐标

必须从 `Hud`（symbol 1540）、`Aimer`（symbol 1431）、`AimerCircle`（symbol 1428）、`Status` 和原部件导出中恢复，不继续保留当前的文字/矩形/Canvas 圆圈占位样式。

| 元素 | 原版关系 | 实现与验收 |
| --- | --- | --- |
| 舞台/安全区 | 原 SWF 为 800×600、30fps | 先建立坐标变换层，确保缩放时 HUD 与场景同源；4:3 对照图不被 16:9 拉伸 |
| 准星 | `Aimer` 的原 circle/line，recoil 改变其展开距离 | 左键连射时十字间距按原公式扩散/收拢；不画手工圆圈替代 |
| 顶部比分/队伍条 | Hud 当前模式/队伍/分数关系 | 模式切换后字段、颜色、位置随原规则变化 |
| 头顶状态条 | `Status.bar_hp/bar_hurt` 与单位坐标 | 敌我受伤、治疗、死亡时位置和比例正确，角色跑动时跟随 |
| 左下职业/生命/等级 | Hud info 和兵种数据 | 根据当前真实兵种/HP/等级变化，不用固定 Medic/85Hp 文本 |
| 经验条 | `bar_exp.width = exp / nextExp * 420` | 击杀/得分后与原长度关系一致 |
| 弹匣/备弹 | `Hud.setAmmoImage` 各枪的盒宽/间隔 | 换枪、射击、换弹后每种枪的格数/备弹变化一致 |
| 命中/弹药/武器图标 | 原子 symbol 与帧 | 命中/换弹/切枪状态可见并不与地图层错位 |

### 阶段 5：枪械、投射物、伤害、拾取、声音和特效

**数据迁移顺序**：先将 `Stats_Guns` 逐枪导出为可追溯数据表，再把该表接入 Player/AI；不能先随手写一把 M4 的近似数值。

每把枪至少记录：原 ID、名称、素材/symbol、弹匣、备弹、射速、单发/连发、散布、后坐、换弹时长、射程、伤害、弹道类型、爆炸半径、声音、可被哪些角色/模式拿到。

武器回归案例：空弹匣开火、连续射击到空、换弹中断、切换武器、对墙命中、对人命中、爆炸边缘、拾取替换、AI 使用、死亡掉落。所有数值先标“已从原版读取”或“未确认”，禁止用估计值伪装成原值。

### 阶段 6：物理、攀爬、地图 action 和 AI 导航

这一阶段直接针对已经反复暴露的问题：浮空、卡墙蹲起、掉坑、AI 发呆、不会跳、卡进地图。

| 机制 | 必须拆出的原版关系 | 必须证明的结果 |
| --- | --- | --- |
| 碰撞体 | Unit 尺寸、脚/头/左右探针、wallMC/NodePhysBox | 角色绝不浮空、穿墙、陷入墙体；调试框不改变真实判定 |
| 斜坡 | 地面采样、脚部高度修正、速度阈值 | 上下坡脚贴地、镜头稳定、AI 同样适用 |
| 蹲起 | 蹲伏高度、站立前头顶空间检测、强制解除条件 | 贴墙/坑边不会永久蹲住；空间不足时不穿顶 |
| 攀爬 | 边缘检测、高度差、前方/上方空位、攀爬帧 | 可爬边缘触发一次完整上爬；不可爬处不吸附 |
| 坑与脱困 | 原地图 action/粉色区域、跳跃触发、离开条件 | 玩家/AI 进入坑后可按原路线跳出，超时有原证据的恢复机制 |
| 导航图 | waypoint、邻接、action box、目标权重 | AI 能跨平台、跳过障碍、追到玩家，而非直线撞墙 |
| 战斗 AI | 发现、视线、瞄准、开火、换枪、撤退/脱困 | 同一种子下可重复；不同兵种/武器行为来源可追踪 |

AI 的完成不以“会往玩家方向移动”计算，必须在 14 图导航回归中记录成功率、平均完成时间、卡死次数和失败位置；任何可重复卡死都视为未通过。

### 阶段 7：模式、战役、挑战、结算和本地进度

| 范围 | 必须迁移的原始内容 | 验收 |
| --- | --- | --- |
| Deathmatch | 计分、出生、时间/分数结束、胜负 | 真实对局从开始到结算 |
| Team Deathmatch | 队伍、队色、队伍出生和总分 | 双队比分、友敌关系正确 |
| Capture the Flag | 旗帜、携带、归还、得分、出生 | 抢旗/死亡掉旗/归还/得分全流程 |
| Domination | 点位、占领进度、积分、颜色 | 多点争夺到结束 |
| Juggernaut | 角色转换、生命/得分特例 | 转换和结算与原规则一致 |
| Campaign 1–15 | 每关任务、条件、敌方配置、失败/胜利、解锁 | 每关不再只是相同 Deathmatch 壳 |
| Challenges 1–15 | 限制、目标、奖励/完成条件 | 每关独立可完成/失败 |
| 本地进度 | 解锁、设置、已完成项 | 刷新后按原规则保存和读取 |

### 阶段 8：最终验证、交接与发布前自检

发布前必须形成以下文件，不靠口头说明：

1. **总台账**：所有 symbol/class/资源/网页模块/测试/截图的映射；
2. **差异清单**：逐项写明“相同、已知差异、证据不足、未做”，禁止把已知缺口藏在 README；
3. **地图验收记录**：14 图 × 10 条回归的通过/失败和截图链接；
4. **动作验收记录**：每兵种/每动作/左右朝向/持枪状态的原版对照；
5. **模式验收记录**：五种模式、15 战役、15 挑战的入口和结束回放；
6. **自动化报告**：`npm test`、覆盖率、浏览器 E2E、资源路径检查均可从干净环境执行；
7. **交接报告**：开发环境、启动命令、目录责任、原件位置、解包命令、运行架构、已知问题、下一步首选任务；
8. **Git 交接**：源代码、必要的可公开资源、文档和提交说明已推远端；大体积或受限原件仅写明本地取得路径与哈希，不混淆其可分发性。

只有阶段 0–8 都有明确的通过记录，才能在项目首页写“1:1 复刻完成”。在此之前项目只能称作“基于原 SWF 证据的迁移进行中”。

## 6. 当前状态、优先顺序与禁止误报

当前已有菜单帧、部分地图资源/节点、Foundry 物理基础、部分 UnitMC 时间轴和 M4 关系；它们是素材与数据基线，**不是 1:1 完成声明**。

当前最近的阻断点是运行时 Canvas 在部分环境被 DOM 地图层合成遮挡。因此先用原 UnitMC 完整 Medic 素材建立了同坐标 DOM 角色覆盖层，保证角色可见；下一步仍要把实时 HUD、头顶血条、完整部件动作和枪械状态放进同样可见、可对照的层，并补回 Canvas/DOM 的统一渲染契约。

**当前执行顺序**：

1. 将原 `Aimer` 与 HUD 子 symbol 接入运行时，并删除所有手绘准星/HUD 占位画法；
2. 以原 800×600 舞台为基线建立统一坐标与缩放契约，再重验 Foundry 画面、镜头与角色层；
3. 从 `UnitMC` 逐状态迁移跑、跳、蹲、攀爬、开火与换弹，不再切整帧人物图；
4. 逐图恢复 wallMC/Node/action 并跑地图回归，优先解决贴墙蹲起、坑内脱困和 AI 跳跃；
5. 最后才扩展完整武器表、AI、模式与战役/挑战。菜单页面可以先展示原资源，但未连到真实规则之前不能标为可玩完成。

**禁止误报清单**：

- 不把“资源路径存在”说成“地图/关卡已经修好”；
- 不把“角色能显示”说成“动作系统已还原”；
- 不把“按钮可点击”说成“原菜单流程已迁移”；
- 不把“AI 会朝目标移动”说成“原导航/跳跃逻辑已完成”；
- 不把“网页能启动”说成“已经可以玩到原版”；
- 不在没有原始证据时新增手绘、估计或通用前端替代内容。
